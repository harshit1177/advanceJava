package com.java.throwsimpl;

public class Questions {

    //1) Difference between Exception and Error
    //2) Try catch finally
    //3) difference between throw and throws keyword
    //4) names some excpetions --> null pointer exception, arithmetic exception, arrayoutof bound exception.
    //5) Types of exception --> Checked exception, unchecked exception
    //            inheritance

   // If the superclass method does not declare an exception,
    // subclass overridden method cannot declare the checked exception.

    //If the superclass method does not declare an exception,
    // subclass overridden method cannot declare the checked exception but can declare unchecked exception.



}
